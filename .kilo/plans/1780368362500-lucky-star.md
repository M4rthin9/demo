# แผนการทำงาน: ลบ Marquee Announcement

## สิ่งที่ต้องทำ

### 1. ลบ HTML ส่วน marquee (status.html)
- ลบ `<div class="marquee-container">` ออกจาก status.html (บรรทัด 21-26)

### 2. ลบ JavaScript marquee functions (src/js/status.js)
- ลบฟังก์ชัน `checkPaymentMarquee()` (บรรทัด 34-50)
- ลบฟังก์ชัน `hideMarquee()` (บรรทัด 52-55)
- ลบการเรียก `checkPaymentMarquee(rows)` ในฟังก์ชัน `doSearch()` (บรรทัด 215)

## รายละเอียด
Marquee ที่อยู่ด้านบนของหน้าจะแสดงรายการ Ref ที่รอชำระเงิน การลบจะทำให้หน้าตาแสดงสะอาดขึ้นโดยไม่มีการประกาศสด